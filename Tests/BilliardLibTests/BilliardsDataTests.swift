//
//  BilliardsDataTests.swift
//  BilliardLibTests
//
//  Created by Fae on 9/15/19.
//

import XCTest

import BilliardLib

class BilliardsDataTests: XCTestCase {

  /*func testMaxTurn() {
    typealias TestCase = (apex: Vec2<k>, expectedDegrees: Singularities<Int>)
    let tests: [TestCase] = [
      (apex: Vec2(k(1, over: 2), k(1, over: 2)),
       expectedDegrees: Singularities(2, 2)),
      (apex: Vec2(k(1, over: 3), k(1, over: 4)),
       expectedDegrees: Singularities(3, 5))
    ]
    for test in tests {
      let data = BilliardsData(apex: test.apex)
      for s in Singularity.all {
        let expected = test.expectedDegrees[s]
        XCTAssertEqual(
          data.maxTurnAroundSingularity[s],
          test.expectedDegrees[s],
          "Max turn around \(s) with apex \(test.apex) should be \(expected)")
      }
    }
  }*/

}
